# p2p-project
This project is a decentralized file-sharing application that allows users (peers) to directly exchange files over a network without the need for a central server. Each node in the system acts both as a client and a server, enabling efficient and scalable data transfer.
